{% extends 'base.htm' %}
{% load tagging_tags %}{% load gravatar %}{% load i18n %}
{% block title %}
{% trans "Entries List" %} | {% if posts.list_by %}::{{ posts.list_by }}{% endif %}
{% endblock%}
{% block content %}
{% if posts.object_list %}
{% for post in posts.object_list %}
	{% tags_for_object post as tag_list %}
<div id="posts">
<div class="post">
<h2><a href="{% url get_post_by_slug post.slug %}">{{ post.title|escape }}</a></h2>
<span class="date">{{ post.created|date:"j N, Y" }}</span>
<div class="entry">{{ post.content|safe }}</div>
<span class="tags">
{% if tag_list %}
{% trans "Tags::" %}{% for tag in tag_list %}<a href="{% url list_post_by_tag tag%}">{{ tag }}</a>{% endfor %}
{% else %}
{% trans "No Tag"%}
{% endif %}
</span>
<span class="num-of-comments"><a title="post's comments" href="#COMMENT">{% blocktrans %}1 comment{% plural %} comments{% endblocktrans %}</a></span>
</div>
{% endfor %}
{% else %}
<div class="message">
<span>{% trans 'No matched entry.' %}</span>
</div>
{% endif %}
</div>
{% endblock %}